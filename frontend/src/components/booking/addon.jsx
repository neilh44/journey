const AddOns = ({ selectedAddOns, onUpdate, onNext, onBack }) => {
    const [availableAddOns, setAvailableAddOns] = useState([]);
  
    useEffect(() => {
      // Fetch available add-ons from API
    }, []);
  
    return (
      <div className="space-y-6">
        {/* Add-ons selection UI */}
      </div>
    );
  };
  
  